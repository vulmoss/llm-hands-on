# 学习实验室

以下为教学样例，不是对当前机器的实时检测结果。

推理服务运行 Ollama，负责加载模型并生成回答或文本向量。
应用负责组装消息、检索资料、执行工具，以及把结果呈现给用户。
两者通过 HTTP 通信，可以在同一台电脑，也可以分别部署。

本实验室先学习直接调用 HTTP，再学习聊天、RAG 和 Agent。
RAG 的索引保存在本地 JSON 文件中。修改文档后需要重新运行 index 命令。
索引使用余弦相似度检索；分数不是答案正确的概率。
