# 核心概念

Token 是模型处理文本的单位，不等于汉字数或英文单词数。
上下文窗口限制一次请求中可以处理的 Token 数量。
聊天应用通过发送历史消息提供上下文，模型不会因为一次聊天而更新参数。

Embedding 把文本转换成数字向量，用于衡量语义相似程度。
RAG 先检索相关资料，再把资料和问题一起交给模型；这个过程不训练模型。
Agent 可以提出工具调用请求，由应用验证参数、执行函数，再把结果返回模型。

GB 与 MB 按十进制换算，1 GB = 1000 MB。
GiB 与 MiB 按二进制换算，1 GiB = 1024 MiB。
