# 验证记录（2026-09-23）

## 已执行

- macOS / Python 3.12：22 项本模块测试通过，覆盖摘要锁、外部镜像拒绝、旧锁保护、远程 Docker context 拒绝、项目归属、保留卷、工具停止、计分、格式错误、同源跳转、PoC 摘要与模板门禁。
- 整仓 pytest：128 项通过；提示词库另 9 项 unittest 通过。Ruff 检查及格式检查、实验目录同步检查通过。
- 24 个 Bash 代码块通过 bash -n；相对文档链接和 JSON 配置语法检查通过。这不等于 Docker Compose 运行验收。
- 真实连接现有 `.15` Ollama / qwen2.5:1.5b；temperature=0、输出预算 768、timeout=180s；V1/V2 各五次顺序调用，另一次工具循环。

## 真实模型结果

| 轮次 | TP | TN | FP | FN | 格式/调用错误 | 含错误的正确率 | 五次客户端耗时 |
|---|---:|---:|---:|---:|---:|---:|---:|
| V1 普通提示 | 0 | 2 | 1 | 2 | 0 | 40% | 12.313s |
| V2 结构化提示 | 0 | 0 | 0 | 0 | 5 | 0% | 7.711s |

原始记录：[V1](evidence/live-v1.json)、[V2](evidence/live-v2.json)、[比较](evidence/comparison.json)。V2 四次输出 Markdown 围栏、一次输出非 JSON 键值文本；均按预先约定严格计 errors，未在看到成绩后放宽解析。即便人工去掉围栏，模型仍多次忽略已给出的正例证据。V1 在 source-map 样例还把材料中的“直接判高危”注释作为依据，构成了具体的材料指令混淆反例。

这五题是小型教学回归集，不能代表总体模型能力；没有重复轮次、随机顺序或置信区间，耗时不用于性能排名。V2 没有改进，保留失败用于第 9 阶段归因练习；不以删题或伪造成功修饰结果。

真实 [Agent trace](evidence/loop-live.json) 有六条可见事件，两次工具调用均由注册表执行并正常停止，但第二次错误地把 1 MiB 而非 1024 MiB 传给换算工具，最终答案错误。**工具运行成功 ≠ 任务答案正确**。应为本题检查计算结果向后传递且最终值为 1 GiB；替身正常结果不能替代真实模型此项不合格。

## 未执行 / 部署 Agent 必须补齐

- 当前 Mac 没有 Docker CLI/daemon；没有拉取三套镜像、没有实际 RepoDigest，也未执行 docker compose config 或容器启动。
- `.16` SSH/Docker 安装条件、资源余量、共享服务状态待核对；没有修改 VM、ESXi 或 TiDB。
- DVWA 实例初始化/登录/SQLi 脚本尚未做真实容器端到端验收；其 token/同源限制/证据字段通过离线测试。
- Juice Shop 实际挑战、浏览器截图、三次独立重做待学习者完成。
- PoC 门禁的 prepare/摘要校验/拒绝修改通过测试；没有代替人批准并执行毕业项目。
- linux/amd64 是 Compose 指定目标；Mac 测试不能证明该平台所有镜像兼容。需要按 DEPLOY 的健康检查和应用验收逐项记录。
- 完全断网镜像搬运存在 RepoDigest 保留限制，见 DEPLOY；默认可执行路径为在线锁定后按摘要启动。

发现环境或模型不满足预期时，记录 FAIL/NOT RUN，而不是把文档命令当作已通过。
